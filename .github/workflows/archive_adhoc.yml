name: iOS CI Build

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: macos-14

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
      
    - name: Set up Xcode version
      run: sudo xcode-select -s /Applications/Xcode_16.2.app  # or adjust version

    - name: Install xcpretty
      run: gem install xcpretty

    - name: Set up keychain and certificates
      run: |
        echo "$P12_BASE64" | base64 --decode > certificate.p12
        security create-keychain -p "" build.keychain
        security default-keychain -s build.keychain
        security unlock-keychain -p "" build.keychain
        security import certificate.p12 -k build.keychain -P "$P12_PASSWORD" -T /usr/bin/codesign
#        security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain

#    - name: Install provisioning profile
#      run: |
#        mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
#        echo "$PROVISIONING_PROFILE_BASE64" | base64 --decode > profile.mobileprovision
#        UUID=$(grep -aA1 UUID profile.mobileprovision | grep -ioE '[-A-F0-9]{36}')
#        cp profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/$UUID.mobileprovision
#
#    - name: Build and archive project
#      run: |
#        xcodebuild clean \
#          -project githubActionsTest.xcodeproj \
#          -scheme githubActionsTest \
#          -configuration Release \
#          -archivePath $PWD/build/githubActionsTest.xcarchive \
#          CODE_SIGN_IDENTITY="${{ secrets.CODE_SIGN_IDENTITY }}" \
#          PROVISIONING_PROFILE_SPECIFIER="$UUID" \
#          DEVELOPMENT_TEAM="${{ secrets.TEAM_ID }}" \
#          PRODUCT_BUNDLE_IDENTIFIER="${{ secrets.APP_ID }}" \
#          archive
#
#    - name: Export .ipa
#      run: |
#        cat > ExportOptions.plist <<EOF
#        <?xml version="1.0" encoding="UTF-8"?>
#        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
#        <plist version="1.0">
#        <dict>
#          <key>method</key>
#          <string>ad-hoc</string>
#          <key>signingStyle</key>
#          <string>manual</string>
#          <key>teamID</key>
#          <string>${{ secrets.TEAM_ID }}</string>
#          <key>provisioningProfiles</key>
#          <dict>
#            <key>${{ secrets.APP_ID }}</key>
#            <string>$UUID</string>
#          </dict>
#          <key>compileBitcode</key>
#          <false/>
#          <key>destination</key>
#          <string>export</string>
#        </dict>
#        </plist>
#        EOF
#
#        xcodebuild -exportArchive \
#          -archivePath $PWD/build/githubActionsTest.xcarchive \
#          -exportOptionsPlist ExportOptions.plist \
#          -exportPath $PWD/build/export
#
#    - name: Upload IPA artifact
#      uses: actions/upload-artifact@v4
#      with:
#        name: ios-app
#        path: build/export/*.ipa
